# 构建上下文排除(加速 docker build,避免传输无关大目录)
**/node_modules
frontend/build
frontend/.env
frontend/.env.*.local
backend/data
backend/music-dl
backend/music-dl.exe
backend/*.exe
# 后端 embed 占位目录里如有真实产物残留也排除(构建阶段会重新拷)
backend/internal/web/frontend_dist/assets
.git
_merge_src
**/.playwright-mcp
*.md
# 测试文件不参与构建(Dockerfile 只 go build 不 go test),
# 排除后可避免工作区里损坏/空的 _test.go 崩掉 go build(曾因 NAS 残留 0 字节 zz_probe_test.go 编译失败)。
**/*_test.go
